.DS_Store
*.log

node_modules/
dist/
.worktrees/
.superpowers/

.env
.env.*
!.env.example
.env.development

# local-only third-party rulebook/art reference — never ship or track
reference/

# owner's untracked screenshot stash — same class as reference/, local-only
ref/

# Wrangler's local dev state (D1/DO sqlite, cache) — machine-local, never shared.
worker/.wrangler/

# generated stats-migration artifacts (contain account ids)
worker/scripts/out/

# match_logs analyzer dumps (contain account ids + full per-move game state)
tools/mlogs/data/
